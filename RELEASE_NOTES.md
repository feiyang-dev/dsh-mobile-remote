# dsh-mobile-remote v1.1.0

**DeepSeek Harness 移动端远程控制插件** —— 在 Web UI 设置页内置「远程控制」：连接二维码、一键开关、在线设备数，让手机通过局域网访问并操控电脑上的 DeepSeek Harness。

## 安装方式

```bash
dsh plugin --profile web add @feiyang666/dsh-mobile-remote
```

安装完成后**重启 dsh web 服务**，打开 `http://127.0.0.1:3080` → **设置 → 远程控制** 即可使用。

## 本版更新（v1.1.0）

> 本版本在 v1.0.0（移动端输入栏适配）基础上新增完整的**远程控制能力**，并从「纯前端注入」升级为「Host + Client 双面插件」。

### 设置页「远程控制」面板（新增）

- **连接二维码**：自动生成 `http://<局域网IP>:<端口>` 的二维码，手机相机 / 浏览器扫码即连
- **开启 / 关闭开关**：一键切换 webserver 监听地址（`0.0.0.0` ↔ `127.0.0.1`），经 dsh 官方 HMR 热重载**无需重启服务**
- **当前连接的设备数量**：移动端心跳上报，实时统计在线设备数
- 局域网地址列表、手机访问地址、配置位置一目了然

### 纯命令行启动支持（新增）

- 官方 CLI 出于安全拒绝 `--host 0.0.0.0`，但本插件通过 profile 的 `cordis.patch.yml`（或 `--patch <overlay>`）写入 `webserver.host: 0.0.0.0`——**命令行用户无需安装桌面端**
- 与桌面端「移动端远程控制」开关实现方式完全一致

### Host API（新增）

- `/__dsh_remote/status`：监听地址 / 端口 / LAN IP / 开关状态 / 在线设备数 / 配置位置
- `/__dsh_remote/toggle`：写入 / 移除 profile patch（幂等，重复操作不产生重复块）
- `/__dsh_remote/heartbeat`：移动端设备心跳上报（90s 过期）
- `/__dsh_remote/qr`：连接二维码 SVG（内联 MIT QR 生成器，零运行时依赖）

### 移动端输入栏适配（保留）

- composer 输入栏窄屏下自动换行分排，选择模型 / 选择安全模式不再粘连
- 选择器限宽防溢出，按钮触控目标 ≥ 40px
- iOS 输入框字号 ≥ 16px 防止聚焦自动缩放

## 已知事项

- 开启后服务暴露到局域网，请仅在可信的家庭 / 办公网络使用，不要暴露到公网
- 设备数统计依赖手机端页面打开后心跳上报，首次打开需等待几秒

## 变更日志

详见 [CHANGELOG.md](./CHANGELOG.md)。
