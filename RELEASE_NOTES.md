# dsh-mobile-remote v1.4.2

**DeepSeek Harness 移动端远程控制插件** —— 在 Web UI 设置页内置「远程控制」：连接二维码、一键开关、在线设备数，让手机通过局域网访问并操控电脑上的 DeepSeek Harness。本版聚焦**移动端体验补全**：修复提示文字常驻屏幕，让设置页在手机上真正可用。

## 安装方式

```bash
dsh plugin --profile web add @feiyang666/dsh-mobile-remote
```

安装完成后**重启 dsh web 服务**，打开 `http://127.0.0.1:3080` → **设置 → 远程控制** 即可使用。

## 本版更新（v1.4.2）

### 📱 修复「提示文字常驻屏幕」

DSH 原生的按钮提示气泡（Tooltip）由 hover / focus 触发，在触屏设备上点击按钮后焦点会一直停留在按钮上，导致「停止」「开始」「关闭菜单栏」等按钮的提示文字一直显示在屏幕上。本版在移动端直接隐藏提示气泡（按钮仍保留无障碍 `aria-label`），界面干净清爽。

### 📲 设置页完整适配移动端

原生设置弹窗是 800px 双栏布局（188px 导航栏 + 内容列），手机上几乎无法使用。本版改为**全屏单栏**：

- 导航栏变成顶部**横向滚动胶囊标签**，触控目标 ≥ 36px；
- 内容列独占剩余空间、边距收紧，各设置区块（含「远程控制」面板）获得完整宽度；
- 通过 `MutationObserver` 识别设置弹窗结构（`role=dialog` 且首个子元素为 `<nav>`）打标适配，**不依赖 harness 内部哈希类名** —— DeepSeek Harness 后续升级后，本插件无需修改即可直接适配。

### 📐 远程控制面板窄屏细节

设备详情 / 外网状态行的长文本在手机上允许换行，行内放不下时自动换行，不再溢出卡片。

## 已知事项

- 开启后服务暴露到公网，**请务必设置远程访问密码**，并仅在可信网络上使用
- 外网隧道需确保服务器 `.env` 的 `FRP_TOKEN` 与 `/www/server/frps/frps.toml` 的 `auth.token` 完全一致，否则面板会明确提示鉴权失败

## 变更日志

详见 [CHANGELOG.md](./CHANGELOG.md)。
