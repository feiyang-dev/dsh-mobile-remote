# dsh-mobile-remote v1.7.0

**DeepSeek Harness 移动端远程控制插件** —— 在 Web UI 设置页内置「远程控制」：连接二维码、一键开关、在线设备数，让手机通过局域网访问并操控电脑上的 DeepSeek Harness。移动端提供上传图片入口、底部抽屉式模型选择、发送状态提示等移动优先体验。

## 安装方式

```bash
dsh plugin --profile web add @feiyang666/dsh-mobile-remote
```

安装完成后**重启 dsh web 服务**，打开 `http://127.0.0.1:3080` → **设置 → 远程控制** 即可使用。

## 本版更新（v1.7.0）

### 🆕 中转服务器 WS 状态推送（长连接替代轮询）

外网隧道开启后，插件端改为通过 **WebSocket 长连接**（`/api/tunnel/ws?bindCode=…`）接收中转服务器的隧道状态实时推送，**替代原来每 5s 的 HTTP 轮询** `/api/tunnel/stats`，中转服务器请求压力降为 0（服务器资源集中用于 frp 隧道中转）。

- **指数退避自动重连**：断线后 2s → 4s → … → 60s 封顶自动恢复；
- **心跳优先走 WS**：WS 已连上时心跳经长连接送达（零 HTTP 请求），未连上才回退 HTTP 兜底；
- **状态查询 60s 节流**：即使移动端每 5s 轮询本机 `/status`，也不会高频打中转服务器；前端优先读 WS 推送缓存；
- **ws 依赖缺失自动降级**：`ws` 未安装时 WS 客户端 no-op，心跳与状态查询回退 HTTP 低频兜底，**功能不中断**。

> ⚠️ 本版新增运行时依赖 `ws`（`^8.18.0`）。通过 `dsh plugin --profile web add` 安装会自动带上；若使用离线 tgz 手动安装，请确保 `ws` 已安装（缺失时插件自动降级，不会崩溃）。

## 历史更新（v1.6.x）

- **v1.6.1**：用量插件等宽表移动端兜底（强制 720px + ≤540px 折叠中间列）
- **v1.6.0**：新增 `GET /__dsh_remote/events` SSE 实时推送端点（设备心跳 / 远程开关 / 外网隧道状态变化即时推送）

## 已知事项

- 开启后服务暴露到公网，**请务必设置远程访问密码**，并仅在可信网络上使用
- 外网隧道需确保服务器 `.env` 的 `FRP_TOKEN` 与 `/www/server/frps/frps.toml` 的 `auth.token` 完全一致，否则面板会明确提示鉴权失败

## 变更日志

详见 [CHANGELOG.md](./CHANGELOG.md)。
