# dsh-mobile-remote v1.7.1

**DeepSeek Harness 移动端远程控制插件** —— 在 Web UI 设置页内置「远程控制」：连接二维码、一键开关、在线设备数，让手机通过局域网访问并操控电脑上的 DeepSeek Harness。移动端提供上传图片入口、底部抽屉式模型选择、发送状态提示等移动优先体验。

## 安装方式

```bash
dsh plugin --profile web add @feiyang666/dsh-mobile-remote
```

安装完成后**重启 dsh web 服务**，打开 `http://127.0.0.1:3080` → **设置 → 远程控制** 即可使用。

## 本版更新（v1.7.1）

### 🆕 外网访问面板：数据通道状态一目了然

「外网访问」卡片新增 **数据通道** 行徽标，一眼看清当前隧道状态数据是 **WS 实时推送**（绿色）还是 **HTTP 兜底（退让模式）**（ws 依赖缺失）、WS 断开 / 重连中等状态，再也不用猜。

### 🆕 运行时长本地每秒刷新

「运行时信息」的**运行时长 / 开机时长 / 隧道已运行**改为秒表效果，每秒跳动，不再依赖 5s 轮询跳变。

### 🎨 远程访问密码界面重做（DeepSeek 官网风格）

- **黑白极简**：深蓝黑背景 + 细微光斑纹理，全套圆角元素（卡片 / 胶囊 logo / 输入框 / 按钮）
- **DSH FishLogo + "deepseek | Harness" 文字 logo** 组合（复刻官网）
- 新增**服务信息面板**：访问地址 / 提供服务者（本机主机名）/ DSH 版本
- 输入密码点击「进入」立即显示**加载动画**（旋转 spinner + 「正在进入…」）

### 🔒 安全增强：清除 / 修改密码需验证当前密码

此前清除密码只需确认框即可一键关闭门禁；现在修改或清除都必须先输入**当前密码**并验证通过，host 端强制校验，防止门禁被未授权篡改。

### 🛠 修复：WS 推送一直"等待推送"

兼容中转服务器对 payload 二次序列化导致的**双重序列化消息**（`"{\"type\":...}"`），插件端自动二次解析，恢复正常接收 `tunnel:status` 推送。

## 历史更新（v1.7.0）

- **v1.7.0**：中转服务器 WS 状态推送客户端（长连接替代每 5s HTTP 轮询，指数退避自动重连，心跳走 WS，ws 依赖缺失自动降级 HTTP 兜底）

## 已知事项

- 开启后服务暴露到公网，**请务必设置远程访问密码**，并仅在可信网络上使用
- 外网隧道需确保服务器 `.env` 的 `FRP_TOKEN` 与 `/www/server/frps/frps.toml` 的 `auth.token` 完全一致，否则面板会明确提示鉴权失败
- 中转服务器若需"WS 推送零双重序列化"，请同步部署 `dsh-update-server` 的 `ws.js` 修复（`send()` 不再重复序列化）

## 变更日志

详见 [CHANGELOG.md](./CHANGELOG.md)。
